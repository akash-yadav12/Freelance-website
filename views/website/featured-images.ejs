<div class="feature-img-container">
    <p class="head-title">Pictures With Various Artists</p>
    <% if(images.length < 1){ %>
        <div style="display: flex; justify-content: center; align-items: center; min-height:70vh;">No Images in the Gallery as of Now</div>
    <% } %>
    <div class="featured-img">
        <% images.forEach(image =>{ %>
            <div class="img-container">
                <img src="<%=image.imagePath%>">
                <p style="line-break: anywhere;"><%=image.description%></p>            
            </div>
        <%})%>
    </div>
</div>
<div id="close">
    &#10006;
</div>